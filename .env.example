# ── Link Hunter Environment ───────────────────────────────────────────────────
# Set HOST_IP to your server's LAN IP so Portainer shows the correct link
HOST_IP=10.10.0.100

# Port exposed on the host (default: 3100)
HOST_PORT=3100
